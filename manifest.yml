applications:
  - name: facilities-api
    path: .
    command: npm run start
    memory: 512M
    env:
      EASEY_FACILITY_MGMT_API_HOST: ((host))
      OPTIMIZE_MEMORY: true
    routes:
      - route: ((host))/api/facility-mgmt
    services:
      - camd-pg-db
