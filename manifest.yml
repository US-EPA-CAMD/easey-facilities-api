applications:
  - name: facilities-api
    path: .
    command: npm run start
    memory: 512M
    env:
      HOST: ((host))
      OPTIMIZE_MEMORY: true
    routes:
      - route: ((host))/api/facility-mgmt
    services:
      - camd-pg-db
